/* blu_light_style.qss*/

/* Stile globale */
* {
    font-family: 'Georgia', Tahoma, Geneva, Verdana, sans-serif;
    font-size: 14px;
    color: #2E2E2E;  /* Colore principale del testo */
}

/* Stile per la finestra principale */
QMainWindow {
    background-color: #F5F5F5;  /* Colore di sfondo principale chiaro */
    border: none;
}

/* Stile per i pulsanti */
QPushButton {
    background-color: #007ACC;  /* Colore di base dei pulsanti (blu) */
    color: #FFFFFF;  /* Testo dei pulsanti in bianco */
    border: none;
    padding: 8px 15px;
    border-radius: 4px;
    font-size: 14px;
}

QPushButton:hover {
    background-color: #005A9E;  /* Colore di hover dei pulsanti */
}

QPushButton:pressed {
    background-color: #003F6B;  /* Colore dei pulsanti quando premuti */
}

QPushButton:disabled {
    background-color: #A6A6A6;  /* Colore dei pulsanti disabilitati */
    color: #FFFFFF;
}

/* Stile per le etichette */
QLabel {
    font-size: 14px;
    color: #2E2E2E;  /* Colore del testo delle etichette */
    padding: 2px;
}

/* Stile per le barre di progressione */
QProgressBar {
    border: 1px solid #B0B0B0;
    border-radius: 4px;
    text-align: center;
    background: #E0E0E0;
    height: 16px;
    font-size: 12px;
    color: #2E2E2E;
}

QProgressBar::chunk {
    background-color: #007ACC;
    width: 20px;
    border-radius: 4px;
}

/* Stile per QTextEdit */
QTextEdit {
    background-color: #FFFFFF;
    border: 1px solid #B0B0B0;
    font-family: 'Georgia', monospace;
    font-size: 14px;
    padding: 10px;
    border-radius: 4px;
    color: #2E2E2E;
    selection-background-color: #D0E7FF;
    selection-color: #2E2E2E;
}

/* Stile per QLineEdit */
QLineEdit {
    background-color: #FFFFFF;
    border: 1px solid #B0B0B0;
    padding: 5px 10px;
    border-radius: 4px;
    font-size: 14px;
    color: #2E2E2E;
    selection-background-color: #D0E7FF;
    selection-color: #2E2E2E;
}

/* Stile per QComboBox */
QComboBox {
    background-color: #FFFFFF;
    border: 1px solid #B0B0B0;
    padding: 5px;
    border-radius: 4px;
    font-size: 14px;
    color: #2E2E2E;
}

QComboBox::drop-down {
    subcontrol-origin: padding;
    subcontrol-position: top right;
    width: 20px;
    border-left-width: 1px;
    border-left-color: #B0B0B0;
    border-left-style: solid;
    border-top-right-radius: 3px;
    border-bottom-right-radius: 3px;
}

QComboBox QAbstractItemView {
    background-color: #FFFFFF;
    border: 1px solid #B0B0B0;
    selection-background-color: #007ACC;
    selection-color: #FFFFFF;
}

/* Stile per QListWidget */
QListWidget {
    background-color: #FFFFFF;
    border: 1px solid #B0B0B0;
    color: #2E2E2E;
}

QListWidget::item {
    padding: 10px;
    border-bottom: 1px solid #E0E0E0;
    background-color: #FFFFFF;
    color: #2E2E2E;
    font-size: 14px;
}

QListWidget::item:selected {
    background-color: #D0E7FF;
    color: #2E2E2E;
}

/* Stile per QTabWidget e le schede */
QTabWidget::pane {
    border: 1px solid #B0B0B0;
    background-color: #F5F5F5;
    border-radius: 4px;
}

QTabBar::tab {
    background: #E0E0E0;
    border: 1px solid #B0B0B0;
    padding: 5px 10px;
    border-radius: 4px;
    margin-right: 2px;
    font-size: 14px;
}

QTabBar::tab:selected {
    background: #FFFFFF;
    border-bottom-color: #FFFFFF;
}

QTabBar::tab:!selected:hover {
    background: #D0D0D0;
}

/* Stile per QGroupBox */
QGroupBox {
    border: 1px solid #B0B0B0;
    border-radius: 4px;
    margin-top: 10px;
    padding-top: 20px
    padding: 10px;
    font-weight: bold;
    color: #2E2E2E;
}

QGroupBox::title {
    subcontrol-origin: margin;
    subcontrol-position: top left;
    left: 10px;
    padding: 0 3px 0 3px;
    background-color: transparent;
}

/* Stile per QMessageBox */
QMessageBox {
    background-color: #F5F5F5;
}

QMessageBox QLabel {
    color: #2E2E2E;
    font-size: 14px;
}

QMessageBox QPushButton {
    background-color: #007ACC;
    color: #FFFFFF;
    border: none;
    padding: 5px 10px;
    border-radius: 4px;
    font-size: 14px;
}

QMessageBox QPushButton:hover {
    background-color: #005A9E;
}

QMessageBox QPushButton:pressed {
    background-color: #003F6B;
}

/* Stile per QRadioButton */
QRadioButton {
    spacing: 5px;
    font-size: 14px;
    color: #2E2E2E;
}

QRadioButton::indicator {
    width: 18px;
    height: 18px;
}

/* Rimuovi o commenta le seguenti righe per utilizzare gli indicatori predefiniti */
/*
QRadioButton::indicator {
    width: 18px;
    height: 18px;
}

QRadioButton::indicator:unchecked {
    image: url(:/icons/radio_unchecked.png);
}

QRadioButton::indicator:checked {
    image: url(:/icons/radio_checked.png);
}
*/

/* Stile per QDateEdit */
QDateEdit {
    background-color: #FFFFFF;
    border: 1px solid #B0B0B0;
    padding: 5px;
    border-radius: 4px;
    font-size: 14px;
    color: #2E2E2E;
}

QDateEdit::drop-down {
    subcontrol-origin: padding;
    subcontrol-position: top right;
    width: 20px;
    border-left-width: 1px;
    border-left-color: #B0B0B0;
    border-left-style: solid;
    border-top-right-radius: 3px;
    border-bottom-right-radius: 3px;
}

QDateEdit QAbstractItemView {
    background-color: #FFFFFF;
    border: 1px solid #B0B0B0;
    selection-background-color: #007ACC;
    selection-color: #FFFFFF;
}

/* Stile per QDockWidget */
QDockWidget {
    titlebar-close-icon: url(:/icons/close.png);
    titlebar-normal-icon: url(:/icons/undock.png);
}

QDockWidget::title {
    background: #E0E0E0;
    padding: 5px;
    font-size: 14px;
    color: #2E2E2E;
}

/* Stile per QToolTip */
QToolTip {
    background-color: #FFFFFF;
    color: #2E2E2E;
    border: 1px solid #B0B0B0;
    padding: 5px;
    border-radius: 4px;
}

/* Stile per QCalendarWidget */
QCalendarWidget {
    border: 1px solid #B0B0B0;  /* Bordo neutro */
    background-color: #F0F0F0;  /* Sfondo leggero neutro */
    border-radius: 5px;  /* Angoli arrotondati */
    font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;  /* Font chiaro e leggibile */
    color: #333333;  /* Colore del testo principale */
}

/* Stile per la parte del mese e dell'anno */
QCalendarWidget QToolButton {
    background-color: #E5E5E5;  /* Colore di sfondo dei pulsanti mese/anno */
    color: #333333;  /* Colore del testo dei pulsanti */
    border: none;  /* Nessun bordo */
    border-radius: 4px;  /* Angoli arrotondati */
    margin: 5px;  /* Margini per il distanziamento */
    padding: 5px;  /* Padding interno */
    font-size: 12px;  /* Dimensione del font */
}

QCalendarWidget QToolButton:hover {
    background-color: #D0D0D0;  /* Colore di sfondo al passaggio del mouse */
}

QCalendarWidget QToolButton:pressed {
    background-color: #C0C0C0;  /* Colore di sfondo quando il pulsante è premuto */
}

/* Stile per la griglia dei giorni */
QCalendarWidget QTableView {
    border: none;  /* Nessun bordo per la griglia */
    background-color: #FAFAFA;  /* Sfondo leggero neutro */
    selection-background-color: #A0A0A0;  /* Colore di sfondo quando un giorno è selezionato */
    selection-color: #FFFFFF;  /* Colore del testo quando un giorno è selezionato */
}

/* Stile per i giorni della settimana */
QCalendarWidget QHeaderView::section {
    background-color: #E0E0E0;  /* Sfondo per i giorni della settimana */
    color: #333333;  /* Colore del testo per i giorni della settimana */
    padding: 5px;  /* Padding per il testo */
    border: none;  /* Nessun bordo */
    font-weight: bold;  /* Testo in grassetto */
}

/* Stile per i giorni disabilitati (come i giorni del mese precedente/successivo) */
QCalendarWidget QAbstractItemView:disabled {
    color: #B0B0B0;  /* Colore testo disabilitato */
}

/* Stile per i giorni di oggi */
QCalendarWidget QTableView::item:selected {
    background-color: #A0A0A0;  /* Sfondo del giorno selezionato */
    color: #FFFFFF;  /* Colore del testo del giorno selezionato */
}

QCalendarWidget QTableView::item:hover {
    background-color: #C0C0C0;  /* Sfondo al passaggio del mouse su un giorno */
}
