/* style.qss BLU DARK THEME */

/* Stile globale */
* {
    font-family: 'Georgia', Tahoma, Geneva, Verdana, sans-serif;
    font-size: 14px;
    color: #F5F5F5;  /* Colore principale del testo chiaro */
}

/* Stile per la finestra principale */
QMainWindow {
    background-color: #2E2E2E;  /* Colore di sfondo principale scuro */
    border: none;
}

/* Stile per i pulsanti */
QPushButton {
    background-color: #005A9E;  /* Colore di base dei pulsanti (blu più scuro) */
    color: #2E2E2E;  /* Testo dei pulsanti in nero */
    border: none;
    padding: 8px 15px;
    border-radius: 4px;
    font-size: 14px;
}

QPushButton:hover {
    background-color: #007ACC;  /* Colore di hover dei pulsanti */
}

QPushButton:pressed {
    background-color: #A6A6A6;  /* Colore dei pulsanti quando premuti */
}

QPushButton:disabled {
    background-color: #3A3A3A;  /* Colore dei pulsanti disabilitati scuri */
    color: #A6A6A6;  /* Testo disabilitato in grigio chiaro */
}

/* Stile per le etichette */
QLabel {
    font-size: 14px;
    color: #F5F5F5;  /* Colore del testo delle etichette chiare */
    padding: 2px;
}

/* Stile per le barre di progressione */
QProgressBar {
    border: 1px solid #4A4A4A;
    border-radius: 4px;
    text-align: center;
    background: #2E2E2E;
    height: 16px;
    font-size: 12px;
    color: #F5F5F5;
}

QProgressBar::chunk {
    background-color: #005A9E;
    width: 20px;
    border-radius: 4px;
}

/* Stile per QTextEdit */
QTextEdit {
    background-color: #3A3A3A;  /* Sfondo scuro */
    border: 1px solid #4A4A4A;
    font-family: 'Georgia', monospace;
    font-size: 14px;
    padding: 10px;
    border-radius: 4px;
    color: #F5F5F5;
    selection-background-color: #005A9E;
    selection-color: #F5F5F5;
}

/* Stile per QLineEdit */
QLineEdit {
    background-color: #3A3A3A;  /* Sfondo scuro */
    border: 1px solid #4A4A4A;
    padding: 5px 10px;
    border-radius: 4px;
    font-size: 14px;
    color: #F5F5F5;
    selection-background-color: #005A9E;
    selection-color: #F5F5F5;
}

/* Stile per QComboBox */
QComboBox {
    background-color: #3A3A3A;  /* Sfondo scuro */
    border: 1px solid #4A4A4A;
    padding: 5px;
    border-radius: 4px;
    font-size: 14px;
    color: #F5F5F5;
}

QComboBox::drop-down {
    subcontrol-origin: padding;
    subcontrol-position: top right;
    width: 20px;
    border-left-width: 1px;
    border-left-color: #4A4A4A;
    border-left-style: solid;
    border-top-right-radius: 3px;
    border-bottom-right-radius: 3px;
}

QComboBox QAbstractItemView {
    background-color: #3A3A3A;
    border: 1px solid #4A4A4A;
    selection-background-color: #005A9E;
    selection-color: #F5F5F5;
}

/* Stile per QListWidget */
QListWidget {
    background-color: #3A3A3A;
    border: 1px solid #4A4A4A;
    color: #F5F5F5;
}

QListWidget::item {
    padding: 10px;
    border-bottom: 1px solid #4A4A4A;
    background-color: #3A3A3A;
    color: #F5F5F5;
    font-size: 14px;
}

QListWidget::item:selected {
    background-color: #005A9E;
    color: #F5F5F5;
}

/* Stile per QTabWidget e le schede */
QTabWidget::pane {
    border: 1px solid #4A4A4A;
    background-color: #2E2E2E;
    border-radius: 4px;
}

QTabBar::tab {
    background: #3A3A3A;
    border: 1px solid #4A4A4A;
    padding: 5px 10px;
    border-radius: 4px;
    margin-right: 2px;
    font-size: 14px;
}

QTabBar::tab:selected {
    background: #2E2E2E;
    border-bottom-color: #2E2E2E;
}

QTabBar::tab:!selected:hover {
    background: #4A4A4A;
}

/* Stile per QGroupBox */
QGroupBox {
    border: 1px solid #4A4A4A;
    padding-top: 20px
    border-radius: 4px;
    margin-top: 10px;
    padding: 10px;
    font-weight: bold;
    color: #F5F5F5;
}

QGroupBox::title {
    subcontrol-origin: margin;
    subcontrol-position: top left;
    left: 10px;
    padding: 0 3px 0 3px;
    background-color: transparent;
}

/* Stile per QMessageBox */
QMessageBox {
    background-color: #2E2E2E;
}

QMessageBox QLabel {
    color: #F5F5F5;
    font-size: 14px;
}

QMessageBox QPushButton {
    background-color: #005A9E;
    color: #2E2E2E;
    border: none;
    padding: 5px 10px;
    border-radius: 4px;
    font-size: 14px;
}

QMessageBox QPushButton:hover {
    background-color: #007ACC;
}

QMessageBox QPushButton:pressed {
    background-color: #A6A6A6;
}

/* Stile per QRadioButton */
QRadioButton {
    spacing: 5px;
    font-size: 14px;
    color: #F5F5F5;
}

QRadioButton::indicator {
    width: 18px;
    height: 18px;
}

/* Stile per QDateEdit */
QDateEdit {
    background-color: #3A3A3A;
    border: 1px solid #4A4A4A;
    padding: 5px;
    border-radius: 4px;
    font-size: 14px;
    color: #F5F5F5;
}

QDateEdit::drop-down {
    subcontrol-origin: padding;
    subcontrol-position: top right;
    width: 20px;
    border-left-width: 1px;
    border-left-color: #4A4A4A;
    border-left-style: solid;
    border-top-right-radius: 3px;
    border-bottom-right-radius: 3px;
}

QDateEdit QAbstractItemView {
    background-color: #3A3A3A;
    border: 1px solid #4A4A4A;
    selection-background-color: #005A9E;
    selection-color: #F5F5F5;
}

/* Stile per QDockWidget */
QDockWidget {
    titlebar-close-icon: url(:/icons/close.png);
    titlebar-normal-icon: url(:/icons/undock.png);
}

QDockWidget::title {
    background: #3A3A3A;
    padding: 5px;
    font-size: 14px;
    color: #F5F5F5;
}

/* Stile per QToolTip */
QToolTip {
    background-color: #3A3A3A;
    color: #F5F5F5;
    border: 1px solid #4A4A4A;
    padding: 5px;
    border-radius: 4px;
}

/* Stile per QCalendarWidget */
QCalendarWidget {
    border: 1px solid #B0B0B0;  /* Bordo neutro */
    background-color: #F0F0F0;  /* Sfondo leggero neutro */
    border-radius: 5px;  /* Angoli arrotondati */
    font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;  /* Font chiaro e leggibile */
    color: #333333;  /* Colore del testo principale */
}

/* Stile per la parte del mese e dell'anno */
QCalendarWidget QToolButton {
    background-color: #E5E5E5;  /* Colore di sfondo dei pulsanti mese/anno */
    color: #333333;  /* Colore del testo dei pulsanti */
    border: none;  /* Nessun bordo */
    border-radius: 4px;  /* Angoli arrotondati */
    margin: 5px;  /* Margini per il distanziamento */
    padding: 5px;  /* Padding interno */
    font-size: 12px;  /* Dimensione del font */
}

QCalendarWidget QToolButton:hover {
    background-color: #D0D0D0;  /* Colore di sfondo al passaggio del mouse */
}

QCalendarWidget QToolButton:pressed {
    background-color: #C0C0C0;  /* Colore di sfondo quando il pulsante è premuto */
}

/* Stile per la griglia dei giorni */
QCalendarWidget QTableView {
    border: none;  /* Nessun bordo per la griglia */
    background-color: #FAFAFA;  /* Sfondo leggero neutro */
    selection-background-color: #A0A0A0;  /* Colore di sfondo quando un giorno è selezionato */
    selection-color: #FFFFFF;  /* Colore del testo quando un giorno è selezionato */
}

/* Stile per i giorni della settimana */
QCalendarWidget QHeaderView::section {
    background-color: #E0E0E0;  /* Sfondo per i giorni della settimana */
    color: #333333;  /* Colore del testo per i giorni della settimana */
    padding: 5px;  /* Padding per il testo */
    border: none;  /* Nessun bordo */
    font-weight: bold;  /* Testo in grassetto */
}

/* Stile per i giorni disabilitati (come i giorni del mese precedente/successivo) */
QCalendarWidget QAbstractItemView:disabled {
    color: #B0B0B0;  /* Colore testo disabilitato */
}

/* Stile per i giorni di oggi */
QCalendarWidget QTableView::item:selected {
    background-color: #A0A0A0;  /* Sfondo del giorno selezionato */
    color: #FFFFFF;  /* Colore del testo del giorno selezionato */
}

QCalendarWidget QTableView::item:hover {
    background-color: #C0C0C0;  /* Sfondo al passaggio del mouse su un giorno */
}
